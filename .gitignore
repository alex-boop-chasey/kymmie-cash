# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# MCP config (may contain API keys)
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lockfile omitted: npm optional-deps bug prunes Linux-only entries
# (@emnapi/*), breaking `npm ci` on the Cloudflare Linux builder.
# Without it, Cloudflare uses `npm install`, which resolves correctly.
package-lock.json
